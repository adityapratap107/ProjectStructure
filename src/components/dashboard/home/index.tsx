import React from 'react';
import {View,Text} from 'react-native';
import Header from '../../common/header';

const Home = ()=>{
    return(
        <View>
            <Header title = {'Home'}/>
        </View>
    )
};

export default Home; 